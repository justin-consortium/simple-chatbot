node_modules/
.env
dist/
.DS_Store

# Plaintext credential exports written by server/scripts/seed-accounts.ts.
# These contain participants' access codes in the clear — NEVER commit them.
credentials*.csv
secrets/
